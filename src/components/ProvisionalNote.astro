---
import { t, type Locale } from '../lib/i18n';

interface Props {
  locale?: Locale;
}

const { locale = 'es' } = Astro.props;
const dict = t(locale);
---

<div class="provisional-note" role="note">
  <strong>{dict.provisionalHeading}:</strong>
  <span><slot /></span>
</div>
